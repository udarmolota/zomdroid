<?xml version="1.0" encoding="utf-8"?>
<!-- Help body for the LIBGL_SHRINK spinner. A dedicated view rather than AlertDialog's plain
     message because the content is a table: the default proportional font makes the columns
     drift and the whole thing reads as a paragraph. Monospace keeps the mode number, the warning
     mark and the description in line, in every translation. -->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp">

        <TextView
            android:id="@+id/texture_shrink_table"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="monospace"
            android:textSize="12sp"
            android:lineSpacingExtra="2dp"
            android:textIsSelectable="true"/>

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="12dp"
            android:layout_marginBottom="12dp"
            android:background="?attr/colorOutlineVariant"/>

        <TextView
            android:id="@+id/texture_shrink_notes"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAppearance="?attr/textAppearanceBodySmall"
            android:textSize="12sp"/>

    </LinearLayout>

</ScrollView>
